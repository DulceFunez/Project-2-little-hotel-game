#include "functions.h"

void resortdata(int const &day, int const &rooms__in_resort, int const &rooms_full, int const &resort_total) {
    std::cout << "\nResort data:\n" << "Day = " << day;
    std::cout << "\nRooms in Resort = " << rooms__in_resort;
    std::cout << "\nRooms full = " << rooms_full;
    std::cout << "\nResort total money = $" << resort_total << "\n";
}